const Discord = require('discord.js');

module.exports = async bot => {
   
    console.log(`━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`);
    console.log(`Le bot est en ligne!`);
    console.log(`Connecter avec (${bot.user.id} // ${bot.user.username})`);
    console.log(`━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`);
}
